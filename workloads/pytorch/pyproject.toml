[project]
name = "pytorch-workload"
version = "0.1.0"
description = "PyTorch GNN benchmark workload for gpu_ext paper (UVM oversubscription)"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "psutil",
    "numpy",
    "matplotlib",
    "torch-geometric",
]
